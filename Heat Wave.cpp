#include <bits/stdc++.h>

using namespace std;

void HeatWave(int x, int y) {
    if (x >= y) {
        cout << "NO" << endl;
    }
    else {
        cout << "YES" << endl;
    }
}

int main() {
    int x, y;
    cin >> x >> y;
    HeatWave(x, y);
    return 0;
}
