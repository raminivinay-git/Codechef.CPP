#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, x, y;
    cin >> a >> x >> y;
    if ((x + y) >= a) {
        cout << "YES";
    }
    else {
        cout << "NO";
    }
    return 0;
}
