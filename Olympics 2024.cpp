#include <bits/stdc++.h>

using namespace std;

void Olympics2024(int g, int s, int b) {
    cout << 15 - (g + s + b);
}

int main() {
    int g, s, b;
    cin >> g >> s >> b;
    Olympics2024(g, s, b);
    return 0;
}
